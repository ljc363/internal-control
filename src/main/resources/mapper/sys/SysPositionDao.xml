<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.internal.modules.sys.dao.SysPositionDao">

	<select id="queryList" resultType="io.internal.modules.sys.entity.SysPositionEntity">
		select t1.*,(select t2.name from sys_position t2 where t2.position_id=t1.parent_id)parentName from sys_position t1 where
			t1.del_flag = 0
		<if test="sql_filter != null">
			and ${sql_filter}
		</if>
	</select>
	
	<select id="queryDetpIdList" resultType="long">
		select position_id from sys_position where parent_id = #{value} and del_flag = 0
	</select>

</mapper>